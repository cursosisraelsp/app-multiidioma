<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso de cámara -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Declaramos que la app puede usar cámara, pero no es obligatoria -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Multiidioma"
        tools:targetApi="31">
        <!-- Activity para lanzar Unity -->
        <activity
            android:name="com.example.multiidioma.utils.UnityLauncherActivity"
            android:exported="false" />
        <!-- Activity principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Multiidioma">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Loader + CameraX + Unity -->
        <activity
            android:name=".LoaderUnityActivity"
            android:exported="false"
            android:theme="@style/Theme.Multiidioma.Fullscreen"/>
        <!-- Loader + CameraX + Unity -->
        <activity
            android:name=".FakeCameraActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"/>

        <!-- UnityPlayerActivity (de Unity) -->
        <activity
            android:name="com.unity3d.player.UnityPlayerGameActivity"
            android:exported="true"
            android:theme="@style/Theme.Multiidioma.Fullscreen"
            android:launchMode="singleTask"
            tools:replace="android:theme,android:exported"/>


    </application>

</manifest>


    <!--<?xml version="1.0" encoding="utf-8"?>
    <manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

        <uses-permission android:name="android.permission.CAMERA" />
        <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Multiidioma"
            tools:targetApi="31">


            <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="AIzaSyA-xEqAweF83RiHDyXiBAwpixQ0wwe3s7Y" />
            <activity
                android:name=".MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme.Multiidioma">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>

            <activity android:name=".LoaderActivity"
                android:exported="false"
                android:theme="@style/Theme.Multiidioma"/>
            <activity
                android:name=".LoaderUnityActivity"
                android:exported="false"
                android:theme="@style/Theme.Multiidioma"/>


        </application>


    </manifest>-->